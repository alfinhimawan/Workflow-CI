name: Iris Classification MLProject
conda_env: conda.yaml

entry_points:
  main:
    parameters:
      n_estimators:
        type: int
        default: 100
      max_depth:
        type: str
        default: "None"
      random_state:
        type: int
        default: 42
    command: >
      python modelling.py
